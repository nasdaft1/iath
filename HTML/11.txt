.form-material table {
    width: 100%; /* Полная ширина таблицы */
    border-collapse: collapse; /* Удаление промежутков между ячейками */
}

.form-material  th {
    /*border: 1px solid #ddd; /* Границы ячеек */
    padding: 8px; /* Внутренние отступы */
    text-align: left; /* Выравнивание текста по левому краю */
    /*border: 1px solid #000;*/
    max-width: 100px; /* Устанавливаем максимальную ширину ячеек */
    overflow: hidden; /* Скрывает контент, выходящий за пределы */
    text-overflow: ellipsis; /* Добавляет троеточие в конце обрезанного текста */
    white-space: nowrap; /* Позволяет перенос на новую строку */
}

.form-material tr:hover {
    background-color: #1d1b1b; /* Фон заголовков таблицы */
    border-radius: 0px;
    
}




.form-material table {
    border-collapse: collapse; /* Убираем двойные линии между ячейками */
    border: 2px solid white; /* Прячем рамку вокруг таблицы */
    padding: 20px; /* Поля вокруг содержимого таблицы */
}


.form-material thead {
    background-color: #f2f2f2;
    position: sticky;
    top: 0;
    z-index: 10;
}


.form-material table td, th{
    padding: 30px; /* Поля вокруг содержимого таблицы */
    
}

.form-material table td, th{
    padding: 3px; /* Поля вокруг содержимого таблицы */
    border: 1px solid black; /* Параметры рамки */
    height: 30px;
}






.table-container-material[data-color = 'select-area-changes'] {
    /* для посетки области куда добавленн или где модифицированн элемент */
    color: var(--theme-area-changes);
}

.table-container-material[data-color = 'select-element-changes']{
    /* для посетки родителя куда добавленн или где модифицированн элемент */
    color: var(--theme-element-changes);
}

.table-container-material[data-color= 'select-element-add']{
    /* для посетки добавленного или модифицированного элемента */
    color: var(--theme-element-add);
}

.table-container-material[data-color = 'select-area-changes']:hover {
    /* для посетки области куда добавленн или где модифицированн элемент */
    color: var(--theme-area-changes);
}

.table-container-material[data-color = 'select-element-changes']:hover{
    /* для посетки родителя куда добавленн или где модифицированн элемент */
    color: var(--theme-element-changes);
}

.table-container-material[data-color= 'select-element-add']:hover{
    /* для посетки добавленного или модифицированного элемента */
    color: var(--theme-element-add);
}
